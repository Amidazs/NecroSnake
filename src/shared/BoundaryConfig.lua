--!strict

-- BoundaryConfig.lua
-- "Boundary further out" – you can tweak these numbers anytime.

local BoundaryConfig = {}

-- Arena boundary radius (studs). Increase to push boundary further out.
BoundaryConfig.ARENA_RADIUS = 800

-- Optional: height for boundary parts (if used).
BoundaryConfig.ARENA_WALL_HEIGHT = 120

return BoundaryConfig
